[project]
name = "aal"
version = "0.1.0"
description = "ai-agent-learn 共享库（Python）"
requires-python = ">=3.10"
dependencies = ["pydantic>=2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aal"]

# 复制为 .env 并填入你自己的密钥，再 `export $(grep -v '^#' .env | xargs)`（或用 dotenv）。
# 默认 mock 模式（AAL_LLM=mock 或不设）不需要任何密钥，全部示例都能离线跑通。

# 切换后端：mock（默认）| anthropic | openai
# AAL_LLM=anthropic

# 真实模型密钥（仅在 AAL_LLM=anthropic / openai 时需要）
# ANTHROPIC_API_KEY=sk-ant-xxx
# OPENAI_API_KEY=sk-proj-xxx
